import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 16,
    backgroundColor: '#111',
    borderRadius: 8,
    margin: 16,
  },
  text: {
    color: '#fff',
    fontSize: 16,
  },
});

export default styles;
